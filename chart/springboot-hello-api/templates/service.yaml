apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot-hello-api.fullname" . }}
  labels:
    {{- include "springboot-hello-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springboot-hello-api.selectorLabels" . | nindent 4 }}
